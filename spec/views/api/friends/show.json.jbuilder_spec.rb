require 'spec_helper'
require 'rails_helper'

describe 'api/friendships/show' do



  it 'renders review json object' do
    # user1 = User.first
    # user2 = User.last
    #
    # assign(:user1, user1)
    # assign(:user2, user2)
    # render
    # expect(view).to render_template(:show)
    # collection_hash = JSON.parse(rendered)
    #
    # expect(collection_hash['users']).to_not be_nil
    # expect(collection_hash['currentUser']).to_not be_nil

  end
end
